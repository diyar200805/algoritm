# Глобальная переменная
x = 10

def change_variable():
    global x  # Указываем, что работаем с глобальной переменной
    x = 20  # Изменяем значение глобальной переменной

# Вызываем функцию, чтобы изменить глобальную переменную
change_variable()

# Печатаем значение глобальной переменной после изменения
print(x)